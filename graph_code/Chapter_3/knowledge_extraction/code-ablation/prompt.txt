请给出段落的主要信息
请给出段落中的不同观点的关联性
请给出段落的核心观点


最后：这段段落的主要观点是（）


1.请给出段落的主要信息
段落：UDP用户数据报首部中检验和的计算方法有些特殊。在计算检验和时，要在UDP  用户 数据报之前增加12个字节的伪首部。所谓“伪首部”是因为这种伪首部并不是UDP 用户数 据报真正的首部。只是在计算检验和时，临时添加在UDP用户数据报前面，得到一个临时的UDP用户数据报。检验和就是按照这个临时的UDP用户数据报来计算的。伪首部既不向 下传送也不向上递交，而仅仅是为了计算检验和。
输出：这段段落讲述了 UDP 用户数据报首部中检验和的计算方法的特殊性。在计算检验和时，需要在 UDP 用户数据报之前增加 12 个字节的伪首部。这个伪首部并不是 UDP 用户数据报真正的首部，而仅仅是为了计算检验和而临时添加在 UDP 用户数据报前面。检验和是按照这个临时的 UDP 用户数据报来计算的，而伪首部并不向下游传送也不向上游递交，仅仅是为了计算检验和。

段落：TCP是面向连接的运输层协议。这就是说，应用程序在使用TCP 协议之前，必须先 建立TCP 连接。在传送数据完毕后，必须释放已经建立的TCP 连接。也就是说，应用进程 之间的通信好像在\"打电话\":通话前要先拨号建立连接，通话结束后要挂机释放连接。
输出：这段话主要介绍了TCP协议的特点，包括面向连接、传输层协议、应用程序建立TCP连接和释放连接等。

2.请给出段落中的不同观点的关联性
段落：UDP用户数据报首部中检验和的计算方法有些特殊。在计算检验和时，要在UDP  用户 数据报之前增加12个字节的伪首部。所谓“伪首部”是因为这种伪首部并不是UDP 用户数 据报真正的首部。只是在计算检验和时，临时添加在UDP用户数据报前面，得到一个临时的UDP用户数据报。检验和就是按照这个临时的UDP用户数据报来计算的。伪首部既不向 下传送也不向上递交，而仅仅是为了计算检验和。
输出：这段话中提到了两个主要观点：1. UDP用户数据报首部的检验和计算方法有些特殊。2. 在计算检验和时，需要增加12个字节的伪首部。这两个观点是相互关联的，都涉及到UDP用户数据报的检验和计算。其中，第一个观点解释了为什么需要增加伪首部，第二个观点则解释了伪首部的具体作用。所以，这两个观点是密切相关的。

段落：TCP是面向连接的运输层协议。这就是说，应用程序在使用TCP 协议之前，必须先 建立TCP 连接。在传送数据完毕后，必须释放已经建立的TCP 连接。也就是说，应用进程 之间的通信好像在\"打电话\":通话前要先拨号建立连接，通话结束后要挂机释放连接。
输出：这段话中提到了两个不同的观点：1. TCP是面向连接的传输层协议。这个观点表明TCP协议是一种用于在网络中传输数据的协议，并且需要在应用程序之间建立连接才能进行数据传输。2. 在使用TCP协议进行应用程序之间的通信时，需要先建立TCP连接，并在数据传输完毕后释放连接。这个观点表明TCP协议提供了一种应用程序之间的通信方式，通过建立TCP连接来实现通信，并在数据传输完成后释放连接。这两个观点是相关的，因为它们都描述了TCP协议在应用程序之间的通信过程中的关键步骤。观点1说明了TCP协议的本质和作用，而观点2则解释了TCP协议在实际应用中的具体实现方式。

3.请给出段落的核心观点
段落：UDP用户数据报首部中检验和的计算方法有些特殊。在计算检验和时，要在UDP  用户 数据报之前增加12个字节的伪首部。所谓“伪首部”是因为这种伪首部并不是UDP 用户数 据报真正的首部。只是在计算检验和时，临时添加在UDP用户数据报前面，得到一个临时的UDP用户数据报。检验和就是按照这个临时的UDP用户数据报来计算的。伪首部既不向 下传送也不向上递交，而仅仅是为了计算检验和。
输出：这段段落讲述了 UDP 数据报的检验和计算方法的特殊性。在计算检验和时，需要增加一个 12 字节的伪首部，这个伪首部并不是 UDP 数据报真正的首部，而是一个临时的 UDP 数据报。检验和计算是按照这个临时的 UDP 数据报进行的，而伪首部并不向下游发送也不向上游递交，仅仅是为了计算检验和。

段落：TCP是面向连接的运输层协议。这就是说，应用程序在使用TCP 协议之前，必须先 建立TCP 连接。在传送数据完毕后，必须释放已经建立的TCP 连接。也就是说，应用进程 之间的通信好像在\"打电话\":通话前要先拨号建立连接，通话结束后要挂机释放连接。
输出：这段段落的核心观点是 TCP 是面向连接的传输层协议，需要先建立连接再进行数据传输，通话结束后释放连接。这就好像在“打电话”，应用程序之间的通信需要先建立连接，通话结束后再释放连接。这种连接-请求-响应模式的通信方式可以确保数据的可靠传输。